# ── SIKANDER OS — ROOT Requirements (Desktop + Setup) ────────────────

# These are OPTIONAL packages for desktop/development setup only.
# For backend server, use: backend/requirements.txt instead.

# ── Desktop Application (Optional) ──────────────────────────────────
pywebview>=5.0
pyinstaller>=6.0

# ── Web Framework Compatibility (Optional) ──────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
starlette>=0.37.0

# ── System Utilities ────────────────────────────────────────────────
psutil>=5.9.0
python-dotenv>=1.0.0
pydantic>=2.7.0

# ── Utils (Fixed Versions) ──────────────────────────────────────────
# FIXED: Aligned with backend/requirements.txt (0.26.0+)
httpx>=0.26.0,<0.28.0
aiofiles>=23.0.0
rich>=13.7.0

# ── Testing (Aligned Versions) ──────────────────────────────────────
# FIXED: Aligned with backend/requirements.txt versions
pytest>=7.4.0
pytest-asyncio>=0.23.0
websockets>=12.0

# ── Note: ───────────────────────────────────────────────────────────
# For FULL backend setup with AI, Security, and Daemon systems,
# run: pip install -r backend/requirements.txt
#
# For FRONTEND setup, run: cd frontend && npm install
#
# For COMBINED (Desktop app), run: pip install -r requirements.txt && npm install
